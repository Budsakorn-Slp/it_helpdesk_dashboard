# ── ความลับ — ห้ามขึ้น git เด็ดขาด ──
.env
.env.*
!.env.example

# ── Python ──
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/

# ── virtualenv ──
.venv/
venv/
venv*/
env/
ENV/

# ── IDE / OS ──
.vscode/
.idea/
*.swp
Thumbs.db
desktop.ini
.DS_Store

# ── ไฟล์ชั่วคราว / สำรอง ──
bak/
*.bak
*_bak*.py
*_bak*.html
*.log

# โฟลเดอร์ที่เผลอ clone ซ้อนเข้ามา — ห้ามเข้า git อีก
it_helpdesk_dashboard_git/

# ── ไฟล์ที่มีเฉพาะบนเซิร์ฟเวอร์ prod ──
app.log*
run.sh
# โมดูลเก่าที่เลิกใช้แล้ว เหลือไว้บนเซิร์ฟเวอร์เผื่ออ้างอิง
queries.py
workflow.py
